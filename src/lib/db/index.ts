/**
 * Database Module
 *
 * This folder contains database-related utilities and clients.
 * Currently uses Prisma ORM with PostgreSQL (Neon serverless).
 *
 * @module lib/db
 */

export { prisma } from "./prisma";
export { default } from "./prisma";
